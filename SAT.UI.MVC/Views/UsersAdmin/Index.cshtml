@model IEnumerable<SAT.UI.MVC.Models.ApplicationUser>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th>

        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Id })*@
                <a href="@Url.Action("Edit", "UsersAdmin", new { id=item.Id })"><i class="fas fa-edit"></i></a>

                <br />

                <a href="@Url.Action("Details", "UsersAdmin", new { id=item.Id })"><i class="fas fa-info-circle"></i></a>

                <br />

                <a href="@Url.Action("Delete", "UsersAdmin", new { id=item.Id })">
                    <i class="fas fa-trash-alt"></i>
                </a>
            </td>
        </tr>
    }

</table>
